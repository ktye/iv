package stats

/*
func TestHist(t *testing.T) {
	N := 100000

	var h Histogram
	h.MaxBins = 60

	for i := 0; i < N; i++ {
		v := rand.NormFloat64()
		h.Push(v)
		//fmt.Printf("%03d [%6.3f, %6.3f] %s\n", len(h.Bins), h.Bins[0].Min, h.Bins[len(h.Bins)-1].Max, h.Sparkline())
	}
	//fmt.Println(h.Sparkline())
	//h.WriteHorizontal(os.Stdout, 10)
	//h.WriteVertical(os.Stdout, 30)
}
*/
